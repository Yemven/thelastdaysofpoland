scripted_gui = {
  weapons_button = {
    window_name = "after_logistics_button"
    context_type = player_context
	#parent_window_token = top_bar

    visible = {
		
    }

    effects = {
		weapons_button_click = {
				if = {
                    limit = {
                        has_country_flag = open_weapons_gui
                    }
                    clr_country_flag = open_weapons_gui
                }
                else = {
                    set_country_flag = open_weapons_gui
                }
		}
		dealer_close_button_click = {
				clr_country_flag = open_weapons_gui
		}
    }

    triggers = {

    }

    dynamic_lists = {
	}

    properties = {
    }

    ai_enabled = {
		always = no
    }

    ai_test_interval = 290  # The higher, the long it takes the ai to do actions
	ai_test_variance = 0.5

    ai_check = {
    }

    ai_check_scope = {
    }

    ai_weights = {
    }
  }
}
