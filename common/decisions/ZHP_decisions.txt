ZHP_deurbanization_category = {

	ZHP_urban_growth = {
		activation = {
			original_tag = ZHP
		}

		days_mission_timeout = 90
		
		timeout_effect = {
			subtract_from_variable = {
				ZHP_deurbanization_progress = 5
			}
			custom_effect_tooltip = ZHP_urban_growth_tt
			hidden_effect = {activate_mission = ZHP_urban_growth}
		}
	}

	ZHP_deurbanization_building = {
		available = {
			OR = {
				has_completed_focus = ZHP_isolation_of_cities
				has_completed_focus = ZHP_mutual_cooperation
			}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			always = yes
		}


		cost = 50

		days_remove = 7

		days_re_enable = 90

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_building"
			custom_effect_tooltip = ZHP_deurbanization_building_tt
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_low_emission = {
		available = {
			OR = {
				has_completed_focus = ZHP_isolation_of_cities
				has_completed_focus = ZHP_mutual_cooperation
			}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			always = yes
		}


		cost = 20

		days_remove = 7

		days_re_enable = 30

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_low_emision"
			#custom_effect_tooltip = ZHP_deurbanization_building_tt
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}

	ZHP_deurbanization_plunder = {
		available = {
			has_completed_focus = ZHP_isolation_of_cities
			321 = {
				is_controlled_by = ZHP
			}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_isolation_of_cities
			321 = {
				is_controlled_by = ZHP
			}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_plunder"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
			custom_effect_tooltip = ZHP_deurbanization_plunder_tt
			hidden_effect = {
				321 = {
					remove_building = {
						type = infrastructure
						level = 5
					}
					remove_building = {
						type = industrial_complex
						level = 1
					}
					remove_building = {
						type = arms_factory
						level = 2
					}
				}
			}
		}
	}

	ZHP_deurbanization_tariffs = {
		available = {
			always = yes
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_mutual_cooperation
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_tariffs"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_belchatow = {
		available = {
			always = yes
		}
		allowed = {
			original_tag = ZHP
		}
		visible = {
			341 = {
				is_core_of = ZHP
			}	
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_belchatow"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}
		}
	}
	
	ZHP_deurbanization_immigrants = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 25 } 
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_immigrants"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 6
			}
		}
	}
	
	ZHP_deurbanization_propaganda = {
		available = {
			has_completed_focus = ZHP_isolation_of_cities
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_isolation_of_cities
		}

		fire_only_once = yes

		cost = 50

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_propaganda"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 4
			}
		}
	}

	ZHP_deurbanization_propaganda_lite = {
		available = {
			has_completed_focus = ZHP_mutual_cooperation
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_mutual_cooperation
		}

		fire_only_once = yes

		cost = 50

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_propaganda_lite"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 4
			}
		}
	}

	ZHP_deurbanization_demolish_old_factories = {
		available = {
			has_completed_focus = ZHP_isolation_of_cities
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_isolation_of_cities
		}

		fire_only_once = yes

		cost = 50

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_demolish_old_factories"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
			random_owned_state = {
				remove_building = {
					type = industrial_complex
					level = 1
				}
			}
			random_owned_state = {
				remove_building = {
					type = industrial_complex
					level = 1
				}
			}
			random_owned_state = {
				remove_building = {
					type = industrial_complex
					level = 1
				}
			}
		}
	}

	ZHP_reforestation_campaign = {
		available = {
			has_completed_focus = ZHP_mutual_cooperation
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			has_completed_focus = ZHP_mutual_cooperation
		}

		fire_only_once = yes

		cost = 50

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_demolish_old_factories"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
			random_owned_state = {
				remove_building = {
					type = industrial_complex
					level = 1
				}
			}
		}
	}
	
	ZHP_deurbanization_gamerLodz = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 25}
			302 = { 
				is_core_of = ZHP
			}
	
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_gamerLodz"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}

			302 = {
				add_manpower = -15270
			}

		}
	}
	
	ZHP_deurbanization_pacification = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_pacification"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_propaganda2 = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_propaganda2"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_medicine = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
			236 = { 
				is_core_of = ZHP
			}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_medicine"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_siberia = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_siberia"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_cityprotection = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_cityprotection"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_terrorism = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_terrorism"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}
		}
	}
	
}
