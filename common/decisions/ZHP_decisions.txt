ZHP_deurbanization_category = {

	ZHP_deurbanization_building = {
		available = {
			OR = {
				has_completed_focus = ZHP_isolation_of_cities
				has_completed_focus = ZHP_mutual_cooperation
				}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			always = yes
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_building"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_tariffs = {
		available = {
			always = yes
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			always = yes
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_tariffs"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_belchatow = {
		available = {
			always = yes
		}
		allowed = {
			original_tag = ZHP
		}
		visible = {
			341 = {
				is_core_of = ZHP
			}	
		}

		fire_only_once = yes

		cost = 50

		days_remove = 3

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_belchatow"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}
		}
	}
	
	ZHP_deurbanization_immigrants = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 25 } 
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_immigrants"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 6
			}
		}
	}
	
	ZHP_deurbanization_propaganda = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_propaganda"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 4
			}
		}
	}
	
	ZHP_deurbanization_gamerLodz = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 25}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 25}
			302 = { 
				is_core_of = ZHP
			}
	
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_gamerLodz"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}

			302 = {
				add_manpower = -15270
			}

		}
	}
	
	ZHP_deurbanization_pacification = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_pacification"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_propaganda2 = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_propaganda2"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_medicine = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 50}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 50}
			236 = { 
				is_core_of = ZHP
			}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_medicine"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 10
			}
		}
	}
	
	ZHP_deurbanization_siberia = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_siberia"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_cityprotection = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_cityprotection"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 5
			}
		}
	}
	
	ZHP_deurbanization_terrorism = {
		available = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		allowed = {
			original_tag = ZHP
		}
		visible = {
			check_variable = { ZHP_deurbanization_progress > 75}
		}

		fire_only_once = yes

		cost = 50

		days_remove = 36

		remove_effect = {
			log = "[GetDateText]: Decision ZHP_deurbanization_terrorism"
			add_to_variable = {
				var = ZHP_deurbanization_progress
				value = 15
			}
		}
	}
	
}
