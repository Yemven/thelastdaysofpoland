coring_category = {
	core_the_state = {
		state_target = yes
		target_root_trigger = {
			always = yes
		}
		ai_will_do = {
			base = 100
		}
		target_array = ROOT.owned_states
		target_trigger = {
			FROM = {
				NOT = { is_core_of = ROOT }
			}
		}
		available = {
			has_political_power > FROM.coring_cost
        }
		days_remove = 30
		custom_cost_trigger = {
			has_political_power > FROM.coring_cost
		}
		custom_cost_text = coring_cost
		cost = coring_cost
		complete_effect = {
			log = "[GetDateText]: Decision core_the_state"
			set_temp_variable = { coring_pp_cost = FROM.coring_cost }
			multiply_temp_variable = { coring_pp_cost = -1 }
			add_political_power = coring_pp_cost
		}
		remove_effect = {
			FROM = {
				add_core_of = ROOT
			}
		}
	}
}