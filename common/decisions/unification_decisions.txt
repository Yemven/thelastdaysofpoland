
unification_category = {

lodz_regional_unification = {

	icon = GFX_decision_icon_default
	
	available = {
		custom_trigger_tooltip = {
			tooltip = controls_lodz_voivodeship
			can_unify_lodz = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { can_unify_lodz = yes }

	complete_effect = {
		if = {
			limit = { tag = ZHP }
				set_cosmetic_tag = ZHP_regional
				news_event = { id = unification.4 }
		}
		else_if = {
			limit = { tag = SAP }
				set_cosmetic_tag = SAP_regional
				news_event = { id = unification.5 }
		}
		else_if = {
			limit = { tag = WML }
				set_cosmetic_tag = WML_regional
				news_event = { id = unification.1 }
		}
		else_if = {
			limit = { tag = BBW }
				set_cosmetic_tag = BBW_regional
				news_event = { id = unification.2 }
		}
		else_if = {
			limit = { tag = ZYD }
				set_cosmetic_tag = ZYD_regional
				news_event = { id = unification.3 }
		}
		else_if = {
			limit = { tag = KRS }
				set_cosmetic_tag = KRS_regional
				news_event = { id = unification.6 }
		}
	}
	ai_will_do = { factor = 1 }
}


lubelskie_regional_unification = {

	icon = GFX_decision_icon_default
	
	available = {
		custom_trigger_tooltip = {
			tooltip = controls_lublin_voivodeship
			can_unify_lubelskie = yes
		}
	}
	
	fire_only_once = yes

	cost = 0

	visible = { can_unify_lubelskie = yes }

	complete_effect = {
		if = {
			limit = { tag = CRN }
				set_cosmetic_tag = CRN_regional
				news_event = { id = unification.7 }
		}
		else_if = {
			limit = { tag = BPD }
				set_cosmetic_tag = BPD_regional
				news_event = { id = unification.13 }
		}
		else_if = {
			limit = { tag = PUL }
				set_cosmetic_tag = PUL_regional
				news_event = { id = unification.8 }
		}
		else_if = {
			limit = { tag = SWD }
				set_cosmetic_tag = SWD_regional
				news_event = { id = unification.10 }
		}
		else_if = {
			limit = { tag = ZUK }
			hidden_effect = {
				set_capital = {
					state = 363
					remember_old_capital = no
				}
			}
			custom_effect_tooltip = ZUK_moves_capital_to_lublin
			set_cosmetic_tag = ZUK_regional
			news_event = { id = unification.11 }
		}
		else_if = {
			limit = { tag = KSN }
				set_cosmetic_tag = KSN_regional
				news_event = { id = unification.9 }
		}
		else_if = {
			limit = { tag = BIL }
				set_cosmetic_tag = BIL_regional
				news_event = { id = unification.12 }
		}
	}
	
	ai_will_do = { factor = 1 }
}


}
