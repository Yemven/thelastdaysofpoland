debug_category = {
	
	ek_dissolution = {

		icon = GFX_decision_generic_form_nation

		available = {
			always = yes
		}

		visible = {
			always = yes
		}


		cost = 1

		ai_will_do = {
			factor = 0
		}
		fire_only_once = yes
		days_remove = 1
		remove_effect  = {
			log = "[GetDateText]: Decision ek_dissolution"
			EKS = {
				country_event = {
					id = eksrozpad.1
					days = 3 
					}
			}			
			EKW = {
				country_event = {
					id = ekwrozpad.1
					days = 7 
					}
			}
			GDA = {
				
				country_event = {
					id = gdarozpad.1
					days = 14 
					}
			}
			KSZ = {

				country_event = {
					id = kszrozpad.1
					days = 12 
					}
			}
		}
	}
	
	zigger_war = {

		icon = GFX_decision_generic_form_nation

		available = {
			always = yes
		}

		visible = {
			always = yes
		}


		cost = 1

		ai_will_do = {
			factor = 0
		}
		fire_only_once = yes
		days_remove = 1
		remove_effect  = {
			log = "[GetDateText]: Decision zigger_war"
			set_global_flag = zigger_war_debug
			ROS = {
				if = { limit = { country_exists = TEU
								 ROOT = { NOT = { tag = TEU } } }
				declare_war_on = {
					target = TEU
					type = annex_everything
				}
				}
				if = { limit = { country_exists = MAL
								 ROOT = { NOT = { tag = MAL } } }
				declare_war_on = {
					target = MAL
					type = annex_everything
				}
				}
				if = { limit = { country_exists = PRU
								 ROOT = { NOT = { tag = PRU } } }
				declare_war_on = {
					target = PRU
					type = annex_everything
				}
				}	
				if = { limit = { country_exists = WAR
								 ROOT = { NOT = { tag = WAR } } }
				declare_war_on = {
					target = WAR
					type = annex_everything
				}
				}
				if = { limit = { country_exists = PZW
								 ROOT = { NOT = { tag = PZW } } }
				declare_war_on = {
					target = PZW
					type = annex_everything
				}
				}	
			}
		}
	}
	
	debug_sapek_sane = {
		cost = 1
		allowed = {
			original_tag = SAP
		}
		
		complete_effect = {
			log = "[GetDateText]: Decision debug_sapek_sane"
			add_to_variable = {sapek_sanity = 100}
		}
		
	}

	debug_sapek_insane = {
		cost = 1
		allowed = {
			original_tag = SAP
		}
		
		complete_effect = {
			log = "[GetDateText]: Decision debug_sapek_insane"
			subtract_from_variable = {sapek_sanity = 100}
		}
	}
	load_war_tree = {

		icon = GFX_decision_generic_form_nation

		available = {
			always = yes
		}

		visible = {
			always = yes
		}


		cost = 1

		ai_will_do = {
			factor = 0
		}
		complete_effect  = {
			log = "[GetDateText]: Decision load_war_tree"
			if = {
				limit = { tag = BBW }
				load_focus_tree = belchatow_war_focus
				}
			else_if = {
				limit = { tag = ZHP }
				load_focus_tree = harcerze_war_focus
				}
			else_if = {
				limit = { tag = ZYD }
				load_focus_tree = zydzew_war_focus
				}
			else_if = {
				limit = { tag = KRS }
				load_focus_tree = krosniewice_war_focus
				}
			else_if = { 
				limit = { tag = SAP }
				load_focus_tree = sapek_war_focus
				}
		}
	}
}