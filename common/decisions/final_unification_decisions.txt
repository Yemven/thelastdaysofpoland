
unification_category = {

poland_unification = {

	icon = GFX_decision_icon_default
	
	available = {
		custom_trigger_tooltip = {
			tooltip = controls_western_poland
			can_unify_poland = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		AND = {
			is_poland_unifier = yes
			OR = {
				has_global_flag = westpol_unified
				has_global_flag = eastpol_unified
				has_global_flag = galicia_unified
				has_global_flag = silesia_unified
			 }
			}
		}

	complete_effect = {
		log = "[GetDateText]: Decision lodz_final_unification"
		set_country_flag = poland_mountain
		

		if = {
			limit = { tag = BBW
					  has_government = reactionary 
					  }
			set_cosmetic_tag = BBW_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.1 }
				}
			}
			custom_effect_tooltip = BBW_unifies_poland_tooltip
		}

		else_if = {
			limit = { tag = BBW
					  has_government = autocracy 
					  }
			set_cosmetic_tag = BBW_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.2 }
				}
			}
			custom_effect_tooltip = BBW_unifies_poland_tooltip
		}

		else_if = {
			limit = { tag = DGR
					  has_government = neonationalism 
					  }
			set_cosmetic_tag = DGR_nazbol_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.3 }
				}
			}
			custom_effect_tooltip = DGR_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = DGR
					  has_government = postpolonism
					  }
			set_cosmetic_tag = DGR_rasta_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.4 }
				}
			}
			custom_effect_tooltip = DGR_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = DGR
					  has_government = marxism_cranism
					  }
			set_cosmetic_tag = DGR_rasta_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.20 }
				}
			}
			custom_effect_tooltip = DGR_unifies_poland_tooltip
		}	


		else_if = {
			limit = { tag = KRS
					  has_government = corporativism }
			set_cosmetic_tag = KRS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.6 }
				}
			}
			custom_effect_tooltip = KRS_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = KRS
					  has_government = autocracy }
			set_cosmetic_tag = KRS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.7 }
				}
			}
			custom_effect_tooltip = KRS_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = KRS
					  has_government = postpolonism }
			set_cosmetic_tag = KRS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.8 }
				}
			}
			custom_effect_tooltip = KRS_unifies_poland_tooltip
		}
		else_if = {
			limit = { tag = SAP
					  has_government = reactionary }
			set_cosmetic_tag = SAP_carlistas_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.9 }
				}
			}
			custom_effect_tooltip = SAP_unifies_poland_tooltip
		}

		
		else_if = {
			limit = { tag = SAP
					 has_government = autocracy }
			set_cosmetic_tag = SAP_insane_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.10 }
				}
			}
			custom_effect_tooltip = SAP_unifies_poland_tooltip
		}

		else_if = {
			limit = { tag = SAP
					  has_government = squattism }
			set_cosmetic_tag = SAP_sane_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.11 }
				}
			}
			custom_effect_tooltip = SAP_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = WML
					  has_country_leader = {
						character = WML_jasmina
						ruling_only = yes
						}
			}
			set_cosmetic_tag = WML_dialog_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.12 }
				}
			}
			custom_effect_tooltip = WML_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = WML
					  has_country_leader = {
						character = WML_szymon_hołownia
						ruling_only = yes
						}
			}
			set_cosmetic_tag = WML_hoiownia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.13 }
				}
			}
			custom_effect_tooltip = WML_unifies_poland_tooltip
		}

		else_if = {
			limit = { tag = ZHP
					  has_government = autocracy }
			set_cosmetic_tag = ZHP_zawisza_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.14 }
				}
			}
			custom_effect_tooltip = ZHP_unifies_poland_tooltip
		}
		else_if = {
			limit = { tag = ZHP
						has_government = postpolonism }
			set_cosmetic_tag = ZHP_zhp_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.15 }
				}
			}
			custom_effect_tooltip = ZHP_unifies_poland_tooltip
		}

		else_if = {
			limit = { tag = ZHP
					  has_government = paternal_conservatism }
			set_cosmetic_tag = ZHP_zhr_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.16 }
				}
			}
			custom_effect_tooltip = ZHP_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { tag = ZYD
					 has_government = marxism_cranism }
			set_cosmetic_tag = ZYD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.18 }
				}
			}
			custom_effect_tooltip = ZYD_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = ZYD
					 has_government = squattism
				}
			set_cosmetic_tag = ZYD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.19 }
				}
			}
			custom_effect_tooltip = ZYD_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = WOD
					 has_government = reactionary
				}
			set_cosmetic_tag = WOD_bekier_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.21 }
				}
			}
			custom_effect_tooltip = WOD_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = WOD
					 has_government = neonationalism
				}
			set_cosmetic_tag = WOD_szczery_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.22 }
				}
			}
			custom_effect_tooltip = WOD_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = WOD
					 has_government = postpolonism
				}
			set_cosmetic_tag = WOD_ragnarok_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.23 }
				}
			}
			custom_effect_tooltip = WOD_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = CIE
					 has_government = euroliberalism
				}
			set_cosmetic_tag = CIE_malysz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.24 }
				}
			}
			custom_effect_tooltip = CIE_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = BSK
					 has_government = freemarketism
				}
			set_cosmetic_tag = BSK_bgolec_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.25 }
				}
			}
			custom_effect_tooltip = BSK_unifies_poland_tooltip
		}
		
		
		else_if = {
			limit = { 
					 tag = BSK
					 has_government = autocracy
				}
			set_cosmetic_tag = BSK_bpierdolec_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.26 }
				}
			}
			custom_effect_tooltip = BSK_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = JSW
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = JSW_nszz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.27 }
				}
			}
			custom_effect_tooltip = JSW_unifies_poland_tooltip
		}	
		
		
		else_if = {
			limit = { 
					 tag = JSW
					 has_government = corporativism
				}
			set_cosmetic_tag = JSW_jsw_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.28 }
				}
			}
			custom_effect_tooltip = JSW_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = JSW
					 has_government = reactionary
				}
			set_cosmetic_tag = JSW_coal_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.29 }
				}
			}
			custom_effect_tooltip = JSW_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = KAT
					 has_government = euroliberalism
				}
			set_cosmetic_tag = KAT_budka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.30 }
				}
			}
			custom_effect_tooltip = KAT_unifies_poland_tooltip
		}	
		
		
		else_if = {
			limit = { 
					 tag = KAT
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = KAT_senior_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.31 }
				}
			}
			custom_effect_tooltip = KAT_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = KAT
					 has_government = freemarketism
				}
			set_cosmetic_tag = KAT_junior_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.32 }
				}
			}
			custom_effect_tooltip = KAT_unifies_poland_tooltip
		}
		
		
		else_if = {
			limit = { 
					 tag = GLI
					 has_government = autocracy
				}
			set_cosmetic_tag = GLI_rajch_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.33 }
				}
			}
			custom_effect_tooltip = GLI_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = GLI
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = GLI_murzinytancujom_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.34 }
				}
			}
			custom_effect_tooltip = GLI_unifies_poland_tooltip
		}		
		
		else_if = {
			limit = { 
					 tag = RAS
				}
			set_cosmetic_tag = RAS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.35 }
				}
			}
			custom_effect_tooltip = RAS_unifies_poland_tooltip
		}	

		else_if = {
			limit = { 
					 tag = CHO
				}
			set_cosmetic_tag = CHO_ras_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.36 }
				}
			}
			custom_effect_tooltip = CHO_unifies_poland_tooltip
		}	

		else_if = {
			limit = { tag = JAW
					  has_country_leader = {
						character = JAW_antifascist_council
						ruling_only = yes
						}
			}
			set_cosmetic_tag = JAW_antifa_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.37 }
				}
			}
			custom_effect_tooltip = JAW_unifies_poland_tooltip
		}


		else_if = {
			limit = { tag = JAW
					  has_country_leader = {
						character = JAW_konieczny
						ruling_only = yes
						}
			}
			set_cosmetic_tag = JAW_razem_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.38 }
				}
			}
			custom_effect_tooltip = JAW_unifies_poland_tooltip
		}		
	
		else_if = {
			limit = { 
					 tag = SOS
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = SOS_wcz_prl_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.39 }
				}
			}
			custom_effect_tooltip = SOS_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = SOS
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = SOS_wcz_good_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.40 }
				}
			}
			custom_effect_tooltip = SOS_unifies_poland_tooltip
		}	
	
		else_if = {
			limit = { 
					 tag = ZAW
				}
			set_cosmetic_tag = ZAW_gowin_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.41 }
				}
			}
			custom_effect_tooltip = ZAW_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = JWK
					 has_government = reactionary
				}
			set_cosmetic_tag = JWK_sparta_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.42 }
				}
			}
			custom_effect_tooltip = JWK_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = JWK
					 has_government = autocracy
				}
			set_cosmetic_tag = JWK_komando_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.43 }
				}
			}
			custom_effect_tooltip = JWK_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = CCH
					 has_government = reactionary
				}
			set_cosmetic_tag = CCH_paulins_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.44 }
				}
			}
			custom_effect_tooltip = CCH_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = CCH
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = CCH_joannites_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.45 }
				}
			}
			custom_effect_tooltip = CCH_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = CCH
					 has_government = postpolonism
				}
			set_cosmetic_tag = CCH_masons_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.46 }
				}
			}
			custom_effect_tooltip = CCH_unifies_poland_tooltip
		}
		
		
		else_if = {
			limit = { 
					 tag = BRZ
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = BRZ_buzzer_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.47 }
				}
			}
			custom_effect_tooltip = BRZ_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = BRZ
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = BRZ_kuba_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.48 }
				}
			}
			custom_effect_tooltip = BRZ_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = BRZ
					 has_government = euroliberalism
				}
			set_cosmetic_tag = BRZ_lewy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.49 }
				}
			}
			custom_effect_tooltip = BRZ_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = LEH
					 has_government = postpolonism
				}
			set_cosmetic_tag = LEH_sanjaj_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.50 }
				}
			}
			custom_effect_tooltip = LEH_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = LCH
					 has_government = reactionary
				}
			set_cosmetic_tag = LCH_bieszk_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.51 }
				}
			}
			custom_effect_tooltip = LCH_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = WAN
					 has_government = squattism
				}
			set_cosmetic_tag = WAN_bieszk_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.52 }
				}
			}
			custom_effect_tooltip = WAN_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = OPO
				}
			set_cosmetic_tag = OPO_cookies_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.53 }
				}
			}
			custom_effect_tooltip = OPO_unifies_poland_tooltip
		}
		
		else_if = {
			limit = { 
					 tag = LMZ
				}
			set_cosmetic_tag = LMZ_piwo_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.58 }
				}
			}
			custom_effect_tooltip = LMZ_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = KON
				}
			set_cosmetic_tag = KON_knur_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.57 }
				}
			}
			custom_effect_tooltip = KON_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = BOM
				}
			set_cosmetic_tag = BOM_major_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.56 }
				}
			}
			custom_effect_tooltip = BOM_unifies_poland_tooltip
		}


		else_if = {
			limit = { 
					 tag = BEL
					 has_government = autocracy
				}
			set_cosmetic_tag = BEL_lukaszenka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.54 }
				}
			}
			custom_effect_tooltip = BEL_unifies_poland_tooltip
		}

		else_if = {
			limit = { 
					 tag = BEL
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = BEL_kartoszki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.55 }
				}
			}
			custom_effect_tooltip = BEL_unifies_poland_tooltip
		}		

	
	}
	ai_will_do = { factor = 1 }
}


}
